spring.application.name=letterboxdClash

# TMDB API Configuration
# Set TMDB_API_KEY environment variable in Railway
# For local development, you can add a fallback after the colon
tmdb.api.key=${TMDB_API_KEY}
